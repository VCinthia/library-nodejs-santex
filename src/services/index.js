const userService = require('./user.service');
const bookService = require('./book.service');
const libraryService = require('./library.service');

module.exports = {bookService, userService, libraryService};